# Python dependencies - Compatible with Compute Canada/Alliance clusters
# Install with: pip install --user -r env/requirements.txt
#
# NOTE: mpi4py is NOT in this list - it's a module on the cluster
#       Load with: module load mpi4py

numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
pytest>=7.4
pytest-cov>=4.1
